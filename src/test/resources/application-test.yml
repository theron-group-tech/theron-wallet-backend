spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/theron_wallet_test
    username: theron
    password: theron
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

asaas:
  api:
    base-url: https://api-sandbox.asaas.com/v3
    key: test-key
    webhook-token: test-webhook-token
    webhook-url: https://test.therongroup.com/api/v1/webhooks/asaas
    retry:
      max-attempts: 3

encryption:
  aes-key: nIlpBopPKbJrzQqLsQcY/EItC724S8pmH2ZI/bTRQrE=

jwt:
  secret: theronTestJwtSecretKeyMustBeAtLeast256BitsLong!!

app:
  admin:
    email: admin@theron.test
    password: TestAdminPass1!

theron:
  security:
    login-max-attempts: 10000
    login-window-seconds: 300

logging:
  level:
    root: WARN
    com.theron.wallet: DEBUG
    org.springframework.web.reactive.function.client: WARN
